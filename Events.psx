Bot.On("Connected", () => {
});

Bot.On('ChannelUserList', () => {
	if (Packet.Channel == '#sourced')
		this.SendMessage('#sourced', 'Online.');
});

Bot.On("Msg", () => {
	Console.WriteLine("[" + Convo.Name + "] <" + User.Nick + "> " + Packet.Message);
});